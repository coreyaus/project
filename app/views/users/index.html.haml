%section.container.content-section.text-center
  .padtop100
    .row
      .col-md-6.col-md-offset-3.padtopmore
        %h1 All Users

        - @users.each_with_index do |user, index|
          .padtop20.padbottom20
            %h3.margin0.padbottom User #{index+1}: #{user.first_name} #{user.last_name}
            = link_to 'Show', user, {:class => "btn btn-default btn-sm"}
            = link_to 'Edit', edit_user_path(user), {:class => "btn btn-default btn-sm"}
            = link_to 'Destroy', user, :class => "btn btn-default btn-sm", :method => :delete, :data => { :confirm => 'Are you sure?' }
